#include <stdio.h>

int main(){
    int sum, sub, multi, divide,m,n,result;
    scanf("%d %d",&m,&n);
    sum = m + n;
    printf("%d \n",sum);
    sub = m - n;
    printf("%d \n",sub);
    multi = m * n;
    printf("%d \n",multi);
    divide = m % n;
    printf("%d \n",divide);
}
